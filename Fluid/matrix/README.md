# Template for matrix example using Microsoft Fluid

## Instructions
* run `npm install` to install dependencies
* run `npm run start` to start the project
* start the autocommit script whenever you continue coding
