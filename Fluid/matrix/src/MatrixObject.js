import ReactDOM from 'react-dom';
import { Matrix } from './Matrix';

export class MatrixObject /* extends ... */ {

    render(div) {
        ReactDOM.render(<Matrix /* TODO: set props */></Matrix>, div)
    }
}